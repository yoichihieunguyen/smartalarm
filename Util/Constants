package com.example.alarm.Util;

public class Constants {
    private Constants() {}

    // Intent extras
    public static final String EXTRA_ALARM_ID = "extra_alarm_id";
    public static final String EXTRA_ALARM_LABEL = "extra_alarm_label";
    public static final String EXTRA_ALARM_TIME = "extra_alarm_time";

    // Intent actions
    public static final String ACTION_ALARM_TRIGGER =
            "com.example.alarm.ACTION_ALARM_TRIGGER";
    public static final String ACTION_ALARM_DISMISS =
            "com.example.alarm.ACTION_ALARM_DISMISS";
    public static final String ACTION_ALARM_SNOOZE =
            "com.example.alarm.ACTION_ALARM_SNOOZE";

    // Notification
    public static final String NOTIFICATION_CHANNEL_ID = "alarm_channel";
    public static final int ALARM_NOTIFICATION_ID = 1001;

    // Defaults
    public static final int INVALID_ALARM_ID = -1;
    public static final int DEFAULT_SNOOZE_MINUTES = 9;

    /*
     * Báo thức tự tắt sau 5 phút nếu người dùng không tương tác.
     */
    public static final long ALARM_AUTO_STOP_MS = 5 * 60 * 1000L;
}
